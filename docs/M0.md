# M0 Bootstrap — bigspec

> Executable checklist for the first implementation sprint.

## Prerequisites

- [ ] Read `docs/build-guide.md` (framework decisions)
- [ ] Read trilogy: constitution → okf-spine → architecture

## Kernel (file-based, no MCP)

- [ ] `kernel/src/validate-okf.ts` — kind-aware validator for 6 M0 kinds + envelope
- [ ] `kernel/src/bp-yaml.ts` — get/set on `specs/cockpit/state.yaml`
- [ ] `kernel/tests/golden/` — one fixture per OKF kind
- [ ] `npm test` green

## Seed skills (5)

Author under `skills/<verb-noun>/SKILL.md` (Agent Skills format). Each declares:
`effort`, `spawn`, `risk`, `movement`, `constitution_version: "1.0.0"`, behavioral `verify:`.

| Skill | Movement | verify: asserts |
|---|---|---|
| `survey-context` | frame | cockpit read + movement detected |
| `elaborate-spec` | specify | valid `okf_kind: story` emitted |
| `plan-work` | plan | valid `tasks` ledger with FAILING checkpoints |
| `develop-tdd` | build | checkpoint PASSING |
| `verify-work` | prove | verification-report or gate pass |
| `using-bigspec` | sustain | bootstrap doc exists (write first) |

## M1 (self-host)

- [ ] `count-bcp` skill — native BCP-Plus (specs in `docs/bcp-plus/`)
- [ ] Gate engine (risk-tier P0–P3)
- [ ] Eval runner (Capstone — with/without-skill Δ)

## Do not yet

- MCP server
- Plugin marketplace publish
- Runtime adapter packs (Cursor/Gemini)
