{
  "name": "bigspec",
  "version": "0.0.0",
  "description": "Spec-driven agentic methodology — constitution, OKF artifacts, fractal loop, native BCP",
  "type": "module",
  "bin": {
    "bigspec": "./kernel/dist/cli.js"
  },
  "scripts": {
    "test": "vitest run",
    "lint": "tsc --noEmit",
    "validate-okf": "node kernel/dist/validate-okf.js"
  },
  "keywords": ["sdd", "agent-skills", "spec-driven", "ai-agents"],
  "license": "MIT",
  "engines": {
    "node": ">=20"
  }
}
